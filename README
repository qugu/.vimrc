Credits to creator of Molokai vim theme: https://github.com/tomasr/molokai

Prerequisites:
Remove `vim-tiny` and install `vim-runtime` and `vim-gui-common`

Use with:

```
wget -O - https://git.io/vbqqq | bash
```
